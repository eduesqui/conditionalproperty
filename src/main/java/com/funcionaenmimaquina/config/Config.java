package com.funcionaenmimaquina.config;

//import org.springframework.context.annotation.Configuration;
/* 
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Bean;


import com.funcionaenmimaquina.conditionalonproperty.MessageService;
import com.funcionaenmimaquina.conditionalonproperty.service.impl.EmailMessageServiceImpl;
import com.funcionaenmimaquina.conditionalonproperty.service.impl.SmsMessageServiceImpl;

import jakarta.annotation.PostConstruct;
*/

//@ConfigurationProperties()
public class Config {
/* 
    private MessageService messageService;

    

    @Bean
    @ConditionalOnProperty(name = "configuration.smsMessageEnabled", havingValue = "true")
    public MessageService smsMessageService() {
        messageService = new SmsMessageServiceImpl();
        return messageService;
    }

    @Bean
    @ConditionalOnProperty(name = "configuration.emailMessageEnabled", havingValue = "true")
    public MessageService emailMessageService() {
        messageService = new EmailMessageServiceImpl();
        return messageService;
    }

*/
    
}
